pipeline {
    agent {
        docker { image 'maven:3.3-jdk-8' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'mvn --version'
                sh 'java --version'
            }
        }
    }
}
